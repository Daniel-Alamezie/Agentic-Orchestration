# Normalise line endings to LF in the repo; let Git convert on checkout
* text=auto eol=lf

# Force binary — never touch line endings in these
*.png  binary
*.jpg  binary
*.gif  binary
*.ico  binary
*.dll  binary
*.exe  binary
*.pdb  binary
*.nupkg binary
